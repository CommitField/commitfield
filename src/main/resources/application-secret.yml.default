custom:
  secret:
    spring:
      data:
        redis:
          password:
    key:
spring:
  security:
    oauth2:
      client:
        registration:
          github:
            client-id:
            client-secret:
            scope:
              - user:email
              - read:user
aws:
  access-key-id:
  secret-access-key:

github:
  token: